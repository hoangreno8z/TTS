# MODELS DIRECTORY (README ONLY)

Thư mục này được sử dụng để chứa các checkpoint trọng số mô hình khi tải về cục bộ.
Tuyệt đối không commit các file nhị phân lớn (.pt, .safetensors, .onnx, .bin) vào Git.
Tất cả model weights được quản lý qua cache hoặc script tải tự động từ Hugging Face Hub.
